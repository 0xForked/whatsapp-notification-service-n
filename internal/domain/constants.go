package domain

const (
	ORIGIN  string = "*"
	HEADERS string = "*"
	METHODS string = "POST, GET"
	//HEADERS string = "Content-Type, Content-Length, Accept-Encoding, X-CSRF-Token, Authorization, accept, origin, Cache-Control, X-Requested-With"

	PONG string = "PONG! {service is running well}"
)
